/*
  按Ctrl+F可格式化
*/
/* 一级标题样式 */
h1 {
  font-size: 1.5em; /* 调整字体大小 */
  font-weight: bold;
  color: #333;
  margin-bottom: 0.25em; /* 紧凑：由 0.5em 改为 0.25em */
  border-bottom: 2px solid #ccc;
  padding: 0.2em; /* 紧凑：由 0.3em 改为 0.2em */
  background-color: #e6f7e6; /* 设置淡绿色背景色 */
  border-radius: 10px; /* 圆角 */
  line-height: 1.25; /* 紧凑行高 */
}

/* 二级标题样式 */
h2 {
  font-size: 1.35em; /* 调整字体大小 */
  font-weight: bold;
  color: #444;
  margin-bottom: 0.25em; /* 紧凑 */
  border-bottom: 1px solid #ccc;
  padding: 0.2em; /* 紧凑 */
  background-color: #e6f7e6; /* 设置淡绿色背景色 */
  border-radius: 10px; /* 圆角 */
  line-height: 1.25; /* 紧凑行高 */
}

/* 三级标题样式 */
h3 {
  font-size: 1.25em; /* 调整字体大小 */
  font-weight: bold;
  color: #555;
  margin-bottom: 0.22em; /* 紧凑 */
  padding: 0.18em; /* 紧凑 */
  background-color: #e6f7e6; /* 设置淡绿色背景色 */
  border-radius: 10px; /* 圆角 */
  line-height: 1.25; /* 紧凑行高 */
}

/* 四级标题样式 */
h4 {
  font-size: 1.15em; /* 调整字体大小 */
  font-weight: bold;
  color: #666;
  margin-bottom: 0.2em; /* 紧凑 */
  padding: 0.16em; /* 紧凑 */
  background-color: #e6f7e6; /* 设置淡绿色背景色 */
  border-radius: 10px; /* 圆角 */
  line-height: 1.25; /* 紧凑行高 */
}


/* 图片样式 */
image {
  display: block;
  max-width: 100%;
  height: auto;
  margin: 0.6em 0; /* 紧凑：由 1em 改为 0.6em */
}

/* 引用样式 */
blockquote {
  font-style: italic;
  color: #555;
  border-left: 5px solid transparent;
  background: linear-gradient(to right, #fffacd, #ffffe0); /* 渐变色背景：从浅黄色到超级淡黄色 */
  margin: 0.6em 0; /* 紧凑：由 1em 改为 0.6em */
  padding: 0.7em; /* 紧凑：由 1em 改为 0.7em */
  border-radius: 10px; /* 圆角 */
}



/* 引用段落样式 */
blockquote_p {
  margin: 0;
  color: #555;
}

/* 段落样式 */
p {
  line-height: 1.55; /* 略紧凑：由 1.6 调整 */
  color: #333;
  margin: 0.6em 0; /* 紧凑：由 1em 改为 0.6em */
}

/* 分割线样式 */
hr {
  border: 0;
  height: 1px;
  background: #ccc;
  margin: 1em 0; /* 紧凑：由 2em 改为 1em */
}

/* 行内代码样式 */
codespan {
  background-color: #f5f5f5;
  border-radius: 3px;
  padding: 0.15em 0.35em; /* 紧凑：由 0.2/0.4 调整 */
  font-family: 'Courier New', Courier, monospace;
  color: #c7254e;
}

/* 粗体样式 */
strong {
  font-weight: bold;
  color: #000;
}

/* 链接样式 */
link {
  color: #1e90ff;
  text-decoration: none;
  border-bottom: 1px dotted #1e90ff;
}

link:hover {
  text-decoration: underline;
}

/* 微信链接样式 */
wx_link {
  color: #07c160;
  text-decoration: none;
  border-bottom: 1px dotted #07c160;
}

wx_link:hover {
  text-decoration: underline;
}

/* 有序列表样式 */
ol {
  margin: 0.6em 0; /* 紧凑：由 1em 改为 0.6em */
  padding-left: 2em;
  list-style-type: decimal;
  color: #333;
}

/* 无序列表样式 */
ul {
  margin: 0.6em 0; /* 紧凑：由 1em 改为 0.6em */
  padding-left: 2em;
  list-style-type: disc;
  color: #333;
}

/* 列表项样式 */
li {
  margin: 0.3em 0; /* 紧凑：由 0.5em 改为 0.3em */
}

/* 代码块样式 */
code {
  display: block;
  background-color: #f5f5f5;
  border-radius: 5px;
  padding: 0.8em; /* 紧凑：由 1em 改为 0.8em */
  font-family: 'Courier New', Courier, monospace;
  color: #c7254e;
  overflow-x: auto;
}
